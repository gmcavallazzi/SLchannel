# slChannel runtime dependencies.
# Prefer `pip install -e .` (see pyproject.toml); this file is kept for
# environments that cannot install the package.

torch>=2.1
numpy>=1.24
pyyaml>=6.0
matplotlib>=3.6

# Optional, Linux + CUDA only: the hand-written gather kernels that make the
# semi-Lagrangian advection fast (semilag_triton.py, eulerian_triton.py).
# Without it the code falls back to torch.compile/eager automatically.
# triton>=3.0
